---
description: Investigate a Luciq production signal end to end (crash, hang, bug report, performance regression, App Store rating drop) and propose a code fix (invokes the luciq-debug skill).
---

Invoke the `luciq-debug` skill via the Skill tool. Pass any arguments the user provided after the command as the skill's `args`.
